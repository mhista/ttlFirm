// Kept only so older imports keep resolving. Both lists are now the same
// data; style them at the call site instead of forking the file.
export { socialLinks, socialLinks as socialLinks2, default } from "@components/common/mediaButtons";
