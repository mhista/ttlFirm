const Section2 = ({children}) => {
  return (
    <div className="w-full relative flex justify-center items-center z-30 bg-zinc-100 text-black">
      {children}
    </div>
  );
};

export default Section2;
