const Section2 = ({children}) => {
  return (
    <div className="w-full relative flex justify-center items-center bg-zinc-100 text-black z-[60] ">
      {children}
    </div>
  );
};

export default Section2;
